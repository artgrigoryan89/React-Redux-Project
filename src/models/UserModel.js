export default class User {
    constructor(name, lastName, login, pass, mail) {
        this.name = name;
        this.lastName = lastName;
        this.login = login;
        this.pass = pass;
        this.mail = mail;
    }
}
